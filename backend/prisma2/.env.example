# URL de la base « lelanation » (distincte de DATABASE_URL utilisée par l’app).
# À définir dans backend/.env ou dans l’environnement pour prisma generate / migrate.
# Créer la base avant : CREATE DATABASE lelanation;
DATABASE_URL_LELANATION="postgresql://USER:PASSWORD@HOST:PORT/lelanation"
