-- Placeholder migration.
-- This directory was accidentally committed without migration.sql, which breaks prisma migrate deploy (P3015).
-- Keep as no-op to preserve migration ordering/history.
SELECT 1;
