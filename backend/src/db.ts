/**
 * @deprecated Use `db/query.js` and `stats/statsPatchQuery.js` instead of Prisma.
 * Kept temporarily so imports resolve during migration cleanup.
 */
export { isDatabaseConfigured, queryRawUnsafe } from './db/query.js'
