# Copier vers .env à la racine : cp .env.example .env
# Fichier lu par docker compose (PostgreSQL local, port hôte 5434).
# Ne jamais committer .env — voir .gitignore.

POSTGRES_USER=lelanation
POSTGRES_PASSWORD=change-me-use-a-strong-password
POSTGRES_DB=lelanation_statistiques
